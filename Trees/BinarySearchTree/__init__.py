"""
=> A binary search tree is defined

For all nodes, { all elements in LST <= node.val < all elements in RST }
                                OR
For all nodes, { all elements in LST < node.val <= all elements in RST }

=> A special property of binary search tree is that the inorder is a sorted array

"""
